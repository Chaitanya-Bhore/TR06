# QueueCraft Environment Variables Configuration

# JWT Secret Key for signing local authentication tokens
JWT_SECRET=your_jwt_secret_key_here

# Server Port (optional, defaults to 5000 for Express, 5000 for FastAPI)
PORT=5000

# Database File Path (optional, defaults to queuecraft.db)
DB_PATH=queuecraft.db
